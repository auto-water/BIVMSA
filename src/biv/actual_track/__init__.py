"""Actual Track: extract A(s), flow(s), compound(s) from code and instructions.

Sub-modules:
- ast_analyzer: Python AST-based taint analysis
- regex_engine: Deterministic regex pattern → capability mapping
- llm_instruction: LLM-based instruction-level capability extraction
"""
